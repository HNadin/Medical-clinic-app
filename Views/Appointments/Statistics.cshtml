@model IEnumerable<Medical_clinic.Models.Appointment>

@{
    ViewData["Title"] = "Statistics";
}

<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
        padding: 0;
    }

    h1 {
        margin: 10px;
        text-align: center;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        background-color: #fff;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        margin: 10px 0;
    }

    table, th, td {
        border: 1px solid #ccc;
    }

    th, td {
        padding: 10px;
        text-align: left;
    }

    th {
        background-color: #f2f2f2;
    }

    a {
        text-decoration: none;
        color: #007BFF;
        margin-right: 10px;
    }
</style>

<h2>Service Statistics</h2>
<table class="table">
    <thead>
        <tr>
            <th>Service Name</th>
            <th>Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewBag.ServiceStatistics)
        {
            <tr>
                <td>@item.ServiceName</td>
                <td>@item.Count</td>
            </tr>
        }
    </tbody>
</table>

<h2>Doctor Statistics</h2>
<table class="table">
    <thead>
    <tr>
        <th>Doctor Name</th>
        <th>Count</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in ViewBag.DoctorStatistics)
    {
        <tr>
            <td>@item.DoctorName</td>
            <td>@item.Count</td>
        </tr>
    }
    </tbody>
</table>


@* <h2>Service Statistics</h2>
<table>
    <thead>
        <tr>
            <th>Service Name</th>
            <th>Appointment Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var serviceStat in ViewBag.ServiceStatistics)
        {
            <tr>
                <td>@serviceStat.Key</td>
                <td>@serviceStat.Value</td>
            </tr>
        }
    </tbody>
</table>

<h2>Doctor Statistics</h2>
<table>
    <thead>
        <tr>
            <th>Doctor Name</th>
            <th>Appointment Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var doctorStat in ViewBag.DoctorStatistics)
        {
            <tr>
                <td>@doctorStat.Key</td>
                <td>@doctorStat.Value</td>
            </tr>
        }
    </tbody>
</table> *@

<p><a asp-action="Index">Back</a></p>